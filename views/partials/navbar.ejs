<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid navbar-height">
        <% if (currentPage==='login' ){ %>
            <a class="navbar-brand" href="/">FOODOOR</a>
            <a class="navbar-brand" href="#">LOGIN PAGE</a>
            <% } else if (currentPage==='signup' ){ %>
                <a class="navbar-brand" href="/">FOODOOR</a>
                <a class="navbar-brand" href="#">SIGNUP PAGE</a>
                <% } else { %>
                <a class="navbar-brand" href="#">FOODOOR</a>
                </div>
                <div class="buttons d-flex text-nowrap gap-4 w-75 justify-content-end me-2 navbar-height">
                <% if (user.user_role==="customer" ){ %>
                    <% if (currentPage==='home' ){ %>
                                <form action="/cart">
                                    <button type="submit" class="btn btn-outline-primary d-flex navbar-height">Your Cart</button>
                                </form>
                                <form action="/categories">
                                    <button type="submit" class="btn btn-outline-warning navbar-height">Categories Section</button>
                                </form>
                                <form action="/past-orders">
                                    <button type="submit" class="btn btn-outline-success navbar-height">Past - Orders</button>
                                </form>
                                <form action="/orders">
                                    <button type="submit" class="btn btn-outline-info navbar-height">Your Ongoing Orders</button>
                                </form>
                    <% } else {%>
                        <form action="/home">
                            <button type="submit" class="btn btn-outline-primary navbar-height">Home</button>
                        </form>
                    <% } %>
                    <% if (currentPage === "order"){ %>
                        <form action="/orders">
                            <button type="submit" class="btn btn-outline-info navbar-height">Back</button>
                        </form>
                    <% } %>
                <% } else if (user.user_role === 'chef'){ %>
                    <% if (currentPage === 'home'){ %>
                        <form action="/add-food">
                            <button type="submit" class="btn btn-outline-info">Add Food</button>
                        </form>
                        <form action="/categories">
                            <button type="submit" class="btn btn-outline-warning">Categories Section</button>
                        </form>
                        <%} else if (currentPage === 'categories'){ %>
                        <form action="/home">
                            <button type="submit" class="btn btn-outline-primary">Home</button>
                        </form>
                        <form action="/add-food">
                            <button type="submit" class="btn btn-outline-info">Add Food</button>
                        </form>
                        <% } else { %>
                        <form action="/home">
                            <button type="submit" class="btn btn-outline-primary">Home</button>
                        </form>
                        <% } %>
                <% } else if (user.user_role === 'admin'){ %>
                    <% if (currentPage === 'admin' ){ %>
                        <form action="/all-payments">
                            <button type="submit" class="btn btn-outline-success">Payments</button>
                        </form>
                        <form action="/add-food">
                            <button type="submit" class="btn btn-outline-primary">Add Food</button>
                        </form>
                        <form action="/categories">
                            <button type="submit" class="btn btn-outline-warning">Categories Section</button>
                        </form>
                        <form action="/all-orders">
                            <button type="submit" class="btn btn-outline-info">All Orders</button>
                        </form>
                        <form action="/users">
                            <button type="submit" class="btn btn-outline-light">All Users</button>
                        </form>
                    <% } else { %>
                        <form action="/admin">
                            <button type="submit" class="btn btn-outline-success">Admin Panel</button>
                        </form>
                    <% } %>
                    <% if (currentPage === 'edit-user'){ %>
                        <form action="/users">
                            <button type="submit" class="btn btn-outline-secondary">Back to Users</button>
                        </form> 
                    <% } %>

                <% } %>
                <form action="/api/logout" method="POST">
                    <button type="submit" class="btn btn-outline-danger">Logout</button>
                </form>
            </div>
        <% } %>
</nav>