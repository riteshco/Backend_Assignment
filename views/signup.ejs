<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>SIGNUP - FOODOOR</title>
</head>

<body class="warm-gradient">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">FOODOOR</a>
            <a class="navbar-brand" href="#">SIGNUP PAGE</a>
        </div>
    </nav>
    <div class="text-center h2 mt-5 fw-bolder">SIGNUP</div>
    <% if(error){ %>
        <div class="h3 m-2 fw-bolder text-center text-danger"><%= error %></div>
    <% } %>
    <form class="container mt-5" action="/register" method="POST">
        <div class="form-group">
            <label for="usr">Username:</label>
            <input name="username" type="text" class="form-control" id="usr" minlength="3" maxlength="16" required>

            <label for="mobile_number">Mobile Number:</label>
            <input name="mobile_number" type="text" minlength="10" maxlength="10" pattern="[6-9]{1}[0-9]{9}" title="Enter a valid 10-digit mobile number starting with 6-9" class="form-control" id="mobile_number" required>

            <label for="email">Email address:</label>
            <input name="email" type="email" class="form-control" id="email" required>

            <label for="password">Password:</label>
            <input name="password" type="password" class="form-control" id="pwd" minlength="8" maxlength="16" placeholder="Length should be >= 8 characters and less than equal to 16 characters" required>
        </div>
        <div>
            <button type="submit" class="btn btn-success">Submit</button>
        </div>
    </form>
</body>

</html>